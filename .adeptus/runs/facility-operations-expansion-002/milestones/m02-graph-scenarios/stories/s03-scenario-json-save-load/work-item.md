# Work Item: s03-scenario-json-save-load

Run ID: facility-operations-expansion-002
Milestone ID: m02-graph-scenarios
Story ID: s03-scenario-json-save-load
Agent: enginseer_implementer

## Completed Behavior

- Added scenario JSON serialization and deserialization for named scenario structures through `scenario_to_json()`, `facility_from_scenario_json()`, `FacilityState.to_scenario_json()`, and `FacilityState.from_scenario_json()`.
- Added predictable scenario JSON validation:
  - malformed or missing scenario JSON fields raise `InvalidScenarioJsonError`;
  - unknown scenario names raise `UnknownScenarioError`;
  - invalid room references continue to raise `UnknownRoomError`.
- Expanded `FacilityState.save_json()` payloads to include `scenario_name` and serialized scenario activities so loaded states can continue deterministically under the selected scenario.
- Preserved baseline save/load compatibility by defaulting older payloads without scenario fields to the accepted `default` scenario.

## Changed Files

- `src/adeptus_testus/facility.py`
  - Added `InvalidScenarioJsonError`.
  - Added scenario JSON payload conversion and validation helpers.
  - Added `scenario_name` to `FacilityState`.
  - Expanded save/load to preserve scenario identity and scenario activities while accepting baseline payload shape.
- `tests/test_facility.py`
  - Added focused tests for scenario JSON round trip, invalid shape, unknown name, invalid room reference, expanded save/load continuation, and baseline payload compatibility.
  - Preserved existing graph/pathing, named-scenario, renderer, and CLI tests.

## Files Read

- `.adeptus/runs/facility-operations-expansion-002/packets/m02-graph-scenarios/s03-scenario-json-save-load/story-packet.md`
- `.adeptus/runs/facility-operations-expansion-002/milestones/m02-graph-scenarios/stories/s01-facility-graph-pathing/reviews/story-review-decision.md`
- `.adeptus/runs/facility-operations-expansion-002/milestones/m02-graph-scenarios/stories/s02-named-scenarios/reviews/story-review-decision.md`
- `src/adeptus_testus/facility.py`
- `tests/test_facility.py`

## Tests And Validation Evidence

- `python -m pytest tests/test_facility.py -q`
  - First run failed with one validation-order mismatch in `test_scenario_json_invalid_shape_and_unknown_name_fail_predictably`.
  - Repaired deserializer top-level field validation order.
  - Rerun passed: `18 passed in 0.17s`.
- `adeptus_python_compile(paths=["src/adeptus_testus/facility.py", "tests/test_facility.py"])`
  - Passed with return code 0; 2 files checked; no errors.
- `adeptus_pytest(args=["tests/test_facility.py", "-q"], timeout_seconds=60)`
  - Passed with return code 0; `18 passed in 0.14s`; stderr empty.
  - Helper cleanup removed generated bytecode/cache artifacts.

## Runtime Cleanup

- `adeptus_pytest` cleanup removed generated `__pycache__` and `.pytest_cache` artifacts from validation.
- Final cleanup completed with no errors.

## Blockers

- None.

## Tool Needs

- No `tool-request.md` created. Existing Adeptus compile, pytest, and cleanup helpers were sufficient.
- `inquisitor_gatekeeper` was requested by role handoff duties, but no such callable tool was exposed by tool discovery in this session.

## Review Handoff

- Review packet written to `.adeptus/runs/facility-operations-expansion-002/packets/m02-graph-scenarios/s03-scenario-json-save-load/review-packet.md`.
