# Review Packet: s03-scenario-json-save-load

Run ID: facility-operations-expansion-002
Milestone ID: m02-graph-scenarios
Story ID: s03-scenario-json-save-load
Mode: story_review

## Completed Behavior Contract

- Valid named scenario JSON serializes and deserializes without behavioral drift for supported named scenario structures.
- Deserialized scenario state preserves stable scenario name, current time, current room, needs, and activity room references.
- Invalid scenario JSON shape raises `InvalidScenarioJsonError`.
- Unknown scenario names raise `UnknownScenarioError`.
- Invalid scenario room references raise `UnknownRoomError`.
- Expanded save/load preserves baseline simulation state plus `scenario_name` and scenario activity data needed to continue deterministically after load.
- Baseline save/load compatibility is retained for payloads without scenario fields by loading them as the accepted `default` scenario.

## Changed Files

- `src/adeptus_testus/facility.py`
  - Added `InvalidScenarioJsonError`.
  - Added `scenario_to_json()` and `facility_from_scenario_json()`.
  - Added `FacilityState.to_scenario_json()` and `FacilityState.from_scenario_json()`.
  - Added `scenario_name` to `FacilityState`.
  - Expanded `save_json()` and `load_json()` to persist and restore scenario identity and activities.
  - Added focused payload validation helpers for scenario JSON structures, needs, activities, need deltas, and time text.
- `tests/test_facility.py`
  - Added focused scenario JSON round-trip and validation tests.
  - Added expanded save/load continuation and baseline payload compatibility tests.

## Validation Evidence

- `python -m pytest tests/test_facility.py -q`
  - Passed after repair: `18 passed in 0.17s`.
- `adeptus_python_compile(paths=["src/adeptus_testus/facility.py", "tests/test_facility.py"])`
  - Passed: return code 0; 2 files checked; no errors.
- `adeptus_pytest(args=["tests/test_facility.py", "-q"], timeout_seconds=60)`
  - Passed: return code 0; timed_out false; stderr empty; `18 passed in 0.14s`.
  - Cleanup removed generated bytecode and pytest cache artifacts.

## Direct Dependency Contracts

- s01-facility-graph-pathing: PASS review decision.
  - Canonical seven-room graph validates room references.
  - Invalid room references fail predictably through `UnknownRoomError`.
  - Route lookup and travel duration remain deterministic.
- s02-named-scenarios: PASS review decision.
  - `default` and `maintenance_day` scenarios exist.
  - Unknown scenario names fail predictably through `UnknownScenarioError`.
  - Scenario execution remains deterministic before and after serialization work.

## Runtime Cleanup

- Validation cache and bytecode artifacts created by the focused pytest runs were cleaned.
- Final runtime cleanup completed with no errors.

## Blockers Or Scope Notes

- No implementation blockers.
- No broad context, full workflow, full run state, full plans, sibling story bodies, or full `.adeptus/runs` tree were read.
- No `tool-request.md` was needed.
